#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0.
 */
int main(void)
{
char a;

if (a = 'a'; a <= 'z', a++)
putchar(a);
putchar('\n')
return (0);
}
